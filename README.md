# Telegram-бот аналитики новостей

Бесплатный Telegram-бот для сбора и анализа новостей из каналов `@finanosint`, `@time_n_John` и других.  
Формирует аналитические записки за день/неделю/месяц/6 месяцев/год и отправляет в указанный канал.

## 🚀 Особенности

- Читает **только русскоязычные** новости (без перевода)
- **Дедупликация** по заголовкам
- **Фильтрация** по ключевым словам (Россия, крипта, SVO и т.д.)
- **Анализ** по вашему ФИНАЛЬНОМУ ПРОМТУ
- Работает **бесплатно** на Render.com
- Без Background Worker — через GitHub Actions или cron

## 📁 Структура

- `bot.py` — читает новости из каналов
- `analyze.py` — формирует аналитику
- `analyze_template.py` — шаблон по вашему промту
- `requirements.txt` — зависимости
- `.env.example` — переменные (не храните в репозитории!)

## 🛠️ Настройка

### 1. Создайте Telegram-бота

- Напишите [@BotFather](https://t.me/BotFather)
- `/newbot` → получите `BOT_TOKEN`

### 2. Создайте Supabase-проект

- Зайдите на [supabase.com](https://supabase.com)
- Создайте проект → получите `SUPABASE_URL` и `SUPABASE_KEY`

### 3. Создайте таблицу в Supabase

```sql
CREATE TABLE news (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title TEXT NOT NULL,
  content TEXT,
  source_channel TEXT NOT NULL,
  pub_date TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  processed BOOLEAN DEFAULT FALSE,
  keywords TEXT[],
  is_duplicate BOOLEAN DEFAULT FALSE,
  original_message_id BIGINT
);

CREATE INDEX idx_news_title ON news(title);
CREATE INDEX idx_news_pub_date ON news(pub_date DESC);
